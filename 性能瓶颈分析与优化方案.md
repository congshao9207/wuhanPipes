## 性能瓶颈分析与优化方案

  1. 最高优先级：余额验真中的排列组合搜索

  文件: src/fileparser/trans_flow/trans_z06_balance_standardization.py:159

  trans_sort() 是一个递归全排列搜索算法，对一天内 N 笔交易，最坏情况是 O(N!)。当某天有 10+
  笔交易且时间顺序错乱时，会探索数万种排列。

  优化方案:

改为拓扑排序/贪心算法，O(N log N)

  def trans_sort(self, df, start=None):
      if df.shape[0] == 0:
          return True

      work_df = df.copy()
      result_rows = []
    
      if start is None:
          # 第一天的起始余额未知，用每笔交易的 last_trans_bal 反推
          # 找到可以作为起点的行
          for _ in range(len(work_df)):
              candidates = work_df if start is None else work_df[work_df['last_trans_bal'] == start]
              if candidates.shape[0] == 0:
                  # 找不到匹配，回退到原逻辑
                  return self._trans_sort_fallback(df, start)
              next_row = candidates.iloc[0]
              result_rows.append(next_row)
              start = next_row['account_balance']
              work_df = work_df[work_df.index != next_row.name]
          return pd.DataFrame(result_rows)
    
      # 已知起始余额，用查找替代递归
      while work_df.shape[0] > 0:
          candidates = work_df[work_df['last_trans_bal'] == start]
          if candidates.shape[0] == 0:
              return False  # 或回退到原递归逻辑
          next_row = candidates.iloc[0]
          result_rows.append(next_row)
          start = next_row['account_balance']
          work_df = work_df[work_df.index != next_row.name]
    
      return pd.concat([basic_df, pd.DataFrame(result_rows)], sort=False)

  核心思路：将 O(N!) 的递归搜索改为 O(N) 的链式查找。预期提升：10-100倍（取决于单日交易量）。

---
  2. 高优先级：行级迭代 + 内层DataFrame查询

  文件: src/fileparser/trans_flow/trans_z09_flow_raw_data.py:69-79

  # 当前：O(n^2) 逐行迭代
  for row in not_full_date_df.itertuples():
      exist_df = not_full_date_df1[(df['trans_amt'] == trans_amt) & ...]
      if exist_df.shape[0] > 0:
          not_full_date_df.drop(getattr(row, 'Index'), inplace=True)

  优化方案:
  # 改为批量 merge 操作，O(n log n)
  merged = not_full_date_df.merge(
      not_full_date_df1[['trans_amt', 'trans_date', 'account_balance', 'opponent_name']],
      on=['trans_amt', 'trans_date', 'account_balance', 'opponent_name'],
      how='left', indicator=True
  )
  duplicate_mask = merged['_merge'] == 'both'
  not_full_date_df = not_full_date_df[~duplicate_mask]

  同文件的 save_raw_data() (第140行) 也逐行构建字典后 add_all。应改为批量 DataFrame 直接写库：

  # 使用 df.to_sql() 一次性写入，避免逐行循环
  insert_df = self.df[col_list].copy()
  insert_df['account_id'] = account_id
  insert_df['out_req_no'] = self.param.get('outReqNo')
  insert_df['create_time'] = self.create_time
  insert_df['update_time'] = self.create_time
  insert_df.to_sql('trans_flow', con=self.db.engine, if_exists='append', index=False, chunksize=5000)

---
  3. 高优先级：SELECT * 加载全部列

  文件: src/portrait/transflow/single_account_portrait/trans_flow.py:151,211 以及 src/view/TransFlow.py:145

  flow_sql = "select * from trans_report_flow where out_req_no in (...)"

  对于有几万条流水数据的表，SELECT * 会拉取所有列（可能 30-50 列）。实际使用的通常只有 15-20 列。

  优化方案: 显式列出需要的列名：
  flow_sql = """
      select trans_time, trans_amt, account_balance, opponent_name,
             trans_channel, trans_type, trans_use, remark,
             mutual_exclusion_label, compatibility_label, relationship,
             currency, opponent_account_no, opponent_account_bank
      from trans_report_flow where out_req_no in (...)
  """

---
  4. 中优先级：双重加载 + 标签映射重复

  文件: trans_flow.py:116 (process()) 和 199 (u_process())

  process() 用于单账户画像和 u_process() 用于联合账户画像独立地从数据库加载同样的 trans_report_flow
  数据，做几乎相同的标签映射和列变换。

  优化方案: 共享已加载的数据，避免重复查询。对同一个 out_req_no_list，只加载一次 DataFrame，然后按 account_id 或
  id_card_no 分配到不同处理路径。

---
  5. 中优先级：8次DataFrame过滤 + groupby + 后续逐行循环

  文件: src/view/TransFlow.py:241-297 (_in_out_order)

  对同一个大 DataFrame 做了 8 次独立过滤，生成 8 个子 DataFrame，每个做 groupby+agg+sort_values，然后又对每个前 20
  名做逐行 df.loc[df['opponent_name'] == name] 赋值。

  优化方案（合并同类项、减少拷贝）：
  @staticmethod
  def _in_out_order(df):
      # 一次性建好条件列，避免多次过滤
      df['_is_valid_person'] = (
          pd.notnull(df.opponent_name) &
          (df.opponent_type == 1) &
          pd.isna(df.loan_type) &
          pd.isna(df.unusual_trans_type) &
          ~df.relationship.astype(str).str.contains('|'.join(STRONGER_RELATIONSHIP)) &
          ~df.opponent_name.astype(str).str.contains('|'.join(UNUSUAL_OPPO_NAME))
      )
      df['_is_valid_company'] = (
          pd.notnull(df.opponent_name) &
          (df.opponent_type == 2) &
          pd.isna(df.loan_type) &
          pd.isna(df.unusual_trans_type) &
          ~df.opponent_name.astype(str).str.contains('|'.join(UNUSUAL_OPPO_NAME)) &
          ~df.relationship.astype(str).str.contains('|'.join(STRONGER_RELATIONSHIP))
      )
      df['_is_income'] = df.trans_amt > 0
      df['_is_expense'] = df.trans_amt < 0

      # 使用 rank 代替逐行循环赋值
      for suffix, cond in [('per', '_is_valid_person'), ('com', '_is_valid_company')]:
          for side, side_cond in [('income', '_is_income'), ('expense', '_is_expense')]:
              subset = df[cond & side_cond]
              cnt_rank = subset.groupby('opponent_name')['trans_amt'].transform('size').rank(
                  method='dense', ascending=False)
              amt_rank = subset.groupby('opponent_name')['trans_amt'].transform('sum').rank(
                  method='dense', ascending=False if side == 'income' else True)
              df.loc[subset.index, f'{side}_cnt_order'] = cnt_rank
              df.loc[subset.index, f'{side}_amt_order'] = amt_rank
      return df

---
  6. 中优先级：pandas apply(lambda) 链

  多个文件中存在连续 apply(lambda x: ...) 调用，可以合并为向量化操作。

  示例（trans_flow.py:173-183）：
  # 当前：4个 apply 调用
  df['label1'] = df['mutual_exclusion_label'].map(self.label_tree)
  df['uni_type'] = df['mutual_exclusion_label'].apply(lambda x: x[4:8])
  df['unusual_trans_type'] = df.apply(lambda x: ..., axis=1)
  df['loan_type'] = df.apply(lambda x: ..., axis=1)
  df['is_sensitive'] = df.apply(lambda x: ..., axis=1)

  # 优化：合并为向量化一次性计算
  df['label1'] = df['mutual_exclusion_label'].map(self.label_tree)
  df['uni_type'] = df['mutual_exclusion_label'].str[4:8]
  df['unusual_trans_type'] = df['label1'].where(df['uni_type'] == '0203')
  df['loan_type'] = df['label1'].where(df['uni_type'] == '0202')
  df['is_sensitive'] = df['unusual_trans_type'].notna() | df['loan_type'].notna()
  df['is_sensitive'] = df['is_sensitive'].replace({False: None, True: 1})

  注意: df.apply(func, axis=1) 是逐行 Python 循环，比向量化慢 10-100 倍。

---
  7. 中优先级：SQL 注入风险（附带影响：无法复用查询计划）

  多处使用字符串格式化构建 SQL：
  # 当前
  acc_sql = "select ... where account_name = '%s' and id_card_no = '%s' ..." % (...)
  flow_sql = "select * from trans_report_flow where out_req_no in (%s)" % ('"'+'","'.join(...)+'"')

  优化方案：使用参数化查询和 IN 语法：
  # 使用 sql_to_df 已支持的 params 参数（见 mysql_reader.py）
  acc_sql = """
      select id as account_id, out_req_no, trans_flow_src_type, file_id
      from trans_account
      where account_name = %(account_name)s
        and id_card_no = %(id_card_no)s
        and bank = %(bank)s
        and account_no = %(account_no)s
        and source_channel = %(source_channel)s
  """
  acc_df = sql_to_df(acc_sql, params={
      "account_name": self.user_name,
      "id_card_no": id_card_no,
      "bank": bank_name,
      "account_no": bank_account,
      "source_channel": self.source_channel
  })

  对于 IN 子句，pandas read_sql 不支持直接传 list，可以使用 SQLAlchemy text() 构造：
  from sqlalchemy import text
  # 或者先构建占位符
  placeholders = ','.join([f':o{i}' for i in range(len(out_req_no_list))])
  flow_sql = f"select * from trans_report_flow where out_req_no in ({placeholders})"
  params = {f'o{i}': v for i, v in enumerate(out_req_no_list)}
  df = sql_to_df(flow_sql, params=params)

---
  8. 低-中优先级：数据库连接池太小

  文件: src/util/mysql_reader.py:10

  __DB_ENGINE = create_engine(DB_URI, pool_size=10, max_overflow=10)

  在画像生成阶段，会顺序执行多个 SQL 查询。但如果并发请求多，池子就容易被占满。

  优化方案： 增大连接池并根据需要调优
  __DB_ENGINE = create_engine(
      DB_URI,
      pool_size=20,
      max_overflow=30,
      pool_pre_ping=True,   # 连接健康检查
      pool_recycle=3600,    # 1小时回收连接
      echo=False
  )

---
  9. 低-中优先级：eval() 动态 ORM 写入

  文件: trans_flow.py:63-86

  transform_class_str 使用 eval() 拼接 SQL 并逐批执行。建议替换为 SQLAlchemy ORM 的批量插入或 df.to_sql()。

---
  10. 架构级优化：并行化与缓存

  当前问题: 单账户画像中，对 N 个关联人的 M 个账户顺序处理（single_portrait.py:47-127），每个账户依次做 6 个画像步骤。

  优化建议：
  - 多线程/进程并行处理：对不同账户的画像计算使用 concurrent.futures.ThreadPoolExecutor（计算密集型用
    ProcessPoolExecutor）
  - 引入缓存层：trans_report_flow 数据加载后缓存起来，避免同一数据多次查询。当前 process() 和 u_process() 分别独立加载
  - 延迟计算：报告展示时只计算需要的画像部分，而不是全部提前算好

---
  优化优先级路线图

| 优先级 | 优化点                        | 预期提升                  | 工作量 |
| ------ | ----------------------------- | ------------------------- | ------ |
| P0     | trans_sort() O(N!) → O(N)     | 10-100x（单日多交易场景） | 2-3天  |
| P1     | 去重 O(N²) → merge            | 10-100x                   | 1天    |
| P1     | SELECT * → 指定列             | 2-5x（减少网络和内存）    | 0.5天  |
| P2     | apply(lambda) → 向量化        | 5-20x                     | 1-2天  |
| P2     | _in_out_order 8次过滤合并     | 2-5x                      | 1天    |
| P2     | 字符串格式 SQL → 参数化SQL    | 安全性 + 边际性能提升     | 1天    |
| P3     | 连接池调优 + 批量写库         | 1-3x（并发场景）          | 0.5天  |
| P3     | 并行化画像计算                | N倍（取决于核心数）       | 2-3天  |
| P3     | 共享加载的 DataFrame 避免重复 | 2x                        | 0.5天  |
| ```    |                               |                           |        |

---
  最推荐优先做的三件事：
  1. 改 trans_sort 递归排列为链式查找 — 单日交易量大时提升最明显
  2. 去掉所有 SELECT * 改为按需选取列 — 改动最小，收益稳定
  3. 将 _in_out_order 的 8 次 DataFrame 过滤合并为带辅助列的向量化计算 — 代码更清晰，性能更好