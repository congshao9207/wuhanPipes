性能瓶颈分析与优化总结                                                                                                
  瓶颈 1（最严重）：get_u_flow_portrait_detail() 被调用了 3 次 — 第 22-24 行                                            
  manual_management_detail = self._get_opponent_detail('manual_management')
  suspected_affiliates_detail  = self._get_opponent_detail('suspected_affiliates')
  related_opponent_detail      = self._get_opponent_detail('related_opponent')

  每个 _get_opponent_detail() 开头都调用一次 get_u_flow_portrait_detail()，导致：
  - 3 次 self.trans_u_flow_portrait.copy()（大数组复制 3 次）
  - 3 次 pd.to_datetime() 全表转换
  - 3 次 .apply(_oppo_name_optimize) + .apply(_oppo_type) —— 这两步是最重的（每行做多次 regex 匹配）

  优化：process() 中调用一次，传给三个分支复用。

  瓶颈 2（严重）：逐对手循环中重复 DataFrame 过滤 — 第 151-172 行

  对每个交易对手（几十到几百个），每次迭代做：
  df.loc[df['oppo_name'] == opponent]     # 第 1 次
  df.loc[(df['oppo_name'] == opponent) & (df['trans_amt'] > 0)]  # 第 2 次
  df.loc[(df['oppo_name'] == opponent) & (df['trans_amt'] < 0)]  # 第 3 次
  df.loc[df['oppo_name'] == opponent]['oppo_type']                # 第 4 次
  df.loc[(df['oppo_name'] == opponent)].groupby('trans_month')... # 第 5 次

  每次 df.loc[df['oppo_name'] == x] 都是全表 O(n) 扫描，对手越多越慢。如果 100 个对手，就需要 500 次全表扫描。

  优化：先用 groupby('oppo_name') 一次聚合出所有对手的总收入/总支出/对手类型/月度明细，再用一个轻量循环构建输出字典。

  瓶颈 3：_oppo_type 中存在无意义的死分支 — 第 83-116 行

  if trans_flow_src_type is None or trans_flow_src_type == 1:
      # ... 100 行代码
  else:
      # ... 完全相同的 100 行代码

  两个分支代码完全一致，if/else 是无效的，但每次都多判断一次。

  优化：去掉条件和多余的 trans_flow_src_type 参数。

  ---

  优化项: 数据预处理调用次数
  改前: 3 次
  改后: 1 次
  收益: 避免 2 次完整数据预处理（最重的正则类判断 + 复制）
  ────────────────────────────────────────
  优化项: 对手收支统计
  改前: 每个对手 5 次 O(n) 过滤
  改后: 1 次 groupby 全量聚合
  收益: O(k×n) → O(n)，k 为对手数
  ────────────────────────────────────────
  优化项: _oppo_type 调用
  改前: axis=1 全表 + 死分支
  改后: 单列 apply + 无分支
  收益: 减少 half 的 regex 调用量