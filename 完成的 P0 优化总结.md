#  完成的 P0 优化总结

  仅修改了一个文件：src/view/TransFlow.py

  变更 1: 数据缓存（第 133-201 行，第 203-243 行）

  get_trans_u_flow_portrait 和 get_mtr_trans_flow_portrait 现在将结果存入 self.cached_data（所有 19
  个视图模块共享的字典）。

  - 第一个视图模块（JsonUnionTitle）触发 init() → 正常执行完整流程：3 次 SQL 查询 + pandas 处理
  - 后续 18 个视图模块直接从缓存读取：仅做一次 .copy() 和 indu_flow 过滤，完全跳过 SQL 和 pandas 处理
  - 使用 .copy() 避免缓存被后续模块意外修改

  节省：每次报告生成减少 18 次 trans_account 查询 + 18 次 trans_report_flow 全表扫描 + 18 次 pandas 特征工程。

  变更 2: _in_out_order 排名算法（第 314-336 行）

  原代码：8 个循环 × 最多 20 次迭代 = 160 次 df.loc[df['opponent_name'] == name] 全表扫描

  优化后：构建一个 name → (column, rank) 的字典，通过 df['opponent_name'].map(rank_map) 一次性完成 4 列赋值

  复杂度从 O(n×m) 降为 O(n+m)，其中 n 为对手方数量，m 为排名列表长度。

  变更 3: apply(axis=1) → np.where（第 187-190 行）

  原代码: df.apply(lambda x: x['label1'] if x['uni_type'] == '0203' else None, axis=1)
  新代码: np.where(df['uni_type'] == '0203', df['label1'], None)
  ────────────────────────────────────────
  原代码: df.apply(lambda x: x['label1'] if x['uni_type'] == '0202' else None, axis=1)
  新代码: np.where(df['uni_type'] == '0202', df['label1'], None)
  ────────────────────────────────────────
  原代码: df.apply(lambda x: 1 if ... else None, axis=1)
  新代码: np.where(cond, 1, None)

  三个 apply(axis=1) 从逐行 Python 循环变为 C 级别向量化操作。

---
  预估效果

  在报告生成场景下（19 个视图模块），优化前每个视图模块都独立加载全部数据，优化后仅有第一个模块加载，后续 18
  个模块命中缓存。对于 n 条流水数据：

  ┌────────────────────────┬───────────────────┬────────┐
  │          操作          │      优化前       │ 优化后 │
  ├────────────────────────┼───────────────────┼────────┤
  │ trans_account 查询     │ ~8 次             │ 1 次   │
  ├────────────────────────┼───────────────────┼────────┤
  │ trans_report_flow 查询 │ 1 次/模块 = 19 次 │ 1 次   │
  ├────────────────────────┼───────────────────┼────────┤
  │ _in_out_order 扫描次数 │ 160 × n           │ 4 × n  │
  ├────────────────────────┼───────────────────┼────────┤
  │ apply(axis=1) 遍历     │ 3 × n             │ 0      │
  └────────────────────────┴───────────────────┴────────┘

  可以测试一下效果，如果满意可以继续实施 P1 优化（消除 JsonUnionConfidenceAnalyse 中的冗余查询、label_logic 缓存等）。