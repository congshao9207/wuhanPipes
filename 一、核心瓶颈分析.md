## 一、核心瓶颈分析

### 1. `SELECT *` 全表查询 + 重复查询同一张大表

`trans_report_flow` 是流水数据的核心大表，但代码中多处使用 `SELECT *` 全量拉取，且同一份数据在不同阶段被重复查询：

- `TransFlowBasic.process()` 查一次 trans_flow.py:150-151
- `TransFlowBasic.u_process()` 又查一次 trans_flow.py:211-212
- `TransModuleProcessor.init()` 再查一次 trans_module_processor.py:39-40
- `TransFlow.get_trans_u_flow_portrait()` 又查一次 TransFlow.py:145-146
- `JsonUnionConfidenceAnalyse.process()` 还查一次 json_u_confidence_analyse.py:66-67

同样的 `trans_report_flow` 大表被查了至少 5 次，每次都是 `SELECT *`，流水量大时每次查询都很慢。

### 2. `label_logic` 等字典表被重复查询

`label_logic` 是一个静态码值表，但在 `TransModuleProcessor.init()` 和 `TransFlow.get_trans_u_flow_portrait()` 中都各查了一次： trans_module_processor.py:44-46 TransFlow.py:155-157

### 3. 大量使用 `df.apply()` 逐行操作

在流水数据的 DataFrame 上大量使用 `apply(lambda, axis=1)` 做逐行判断，这在 Pandas 中是最慢的操作方式：



```
# 这三行在多个文件中重复出现，每行都是逐行遍历  
df['unusual_trans_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0203' else None, axis=1)  
df['loan_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0202' else None, axis=1)  
df['is_sensitive'] = df.apply(  
    lambda x: 1 if pd.notna(x['unusual_trans_type']) or pd.notna(x['loan_type']) else None, axis=1)
```

TransFlow.py:172-175

### 4. `_in_out_order()` 中 O(n*m) 的 `df.loc` 循环赋值

这个方法对 8 个列表各做一次循环，每次循环内用 `df.loc[df['opponent_name'] == name]` 做全表扫描，复杂度为 O(8 * 20 * n)： TransFlow.py:283-298

### 5. View 层串行处理 20+ 个视图

`V51001.transform()` 中 20+ 个视图处理器是串行执行的，每个视图内部又各自查数据库： v51001.py:121-126

### 6. `_time_interval()` 每次都 `df.copy()` 全量复制

每次调用都复制整个 DataFrame，且同一份数据被调用多次（1年、2年各一次）： trans_flow.py:256-275

### 7. 数据库连接池偏小

连接池只有 `pool_size=10, max_overflow=10`，并发时容易成为瓶颈： mysql_reader.py:13

------

## 二、优化建议

### 优先级 P0：消除重复查询（预计效果最大）

将 `trans_report_flow` 的查询结果缓存到 `data_repository`/`cached_data` 中，整个报告生命周期只查一次。



```
# 在 data_repository 中缓存  
if 'trans_report_flow_df' not in cached_data:  
    cached_data['trans_report_flow_df'] = sql_to_df(sql=flow_sql)  
df = cached_data['trans_report_flow_df']
```

涉及文件：

- `src/mapping/trans_module_processor.py`
- `src/view/TransFlow.py`
- `src/portrait/transflow/single_account_portrait/trans_flow.py`
- `src/view/p08001_v/json_u_confidence_analyse.py`

### 优先级 P0：SQL 只查需要的列

将 `SELECT * FROM trans_report_flow` 改为只查需要的列，减少网络传输和内存占用：



```
SELECT out_req_no, trans_time, trans_amt, account_balance, opponent_name,  
       mutual_exclusion_label, compatibility_label, remark, trans_use, ...  
FROM trans_report_flow WHERE out_req_no IN (...)
```

### 优先级 P1：用向量化操作替代 `apply(axis=1)`



```
# Before (慢):  
df['unusual_trans_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0203' else None, axis=1)  
df['loan_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0202' else None, axis=1)  
df['is_sensitive'] = df.apply(  
    lambda x: 1 if pd.notna(x['unusual_trans_type']) or pd.notna(x['loan_type']) else None, axis=1)  
  
# After (快 10-100x):  
df['unusual_trans_type'] = df['label1'].where(df['uni_type'] == '0203')  
df['loan_type'] = df['label1'].where(df['uni_type'] == '0202')  
df['is_sensitive'] = pd.notna(df['unusual_trans_type']) | pd.notna(df['loan_type'])  
df['is_sensitive'] = df['is_sensitive'].where(df['is_sensitive'], other=None).astype('Int64')
```

### 优先级 P1：优化 `_in_out_order()` 循环赋值

用 `map` 替代循环 `df.loc`：



```
# Before: 8个循环 * 20次 df.loc 全表扫描  
for i in range(len(income_per_cnt_list)):  
    df.loc[df['opponent_name'] == income_per_cnt_list[i], 'income_cnt_order'] = i + 1  
  
# After: 用 map 一次完成  
income_per_cnt_map = {name: i+1 for i, name in enumerate(income_per_cnt_list)}  
df['income_cnt_order'] = df['opponent_name'].map(income_per_cnt_map)
```

### 优先级 P2：缓存 `label_logic` 字典表

这是一个静态码值表，应在应用启动时加载一次并缓存：



```
# 全局缓存  
_label_cache = None  
def get_label_dict():  
    global _label_cache  
    if _label_cache is None:  
        label_df = sql_to_df("select label_code, label_explanation from label_logic where label_type = 'LABEL'")  
        _label_cache = {row.label_code: row.label_explanation for row in label_df.itertuples()}  
    return _label_cache
```

### 优先级 P2：`_time_interval()` 避免 `df.copy()`



```
@staticmethod  
def _time_interval(df, year=1):  
    # 直接用布尔索引返回视图，不做 copy  
    filter_col = 'trans_date' if 'trans_date' in df.columns else 'trans_time'  
    max_date = df[filter_col].max()  
    return df[df[filter_col] >= max_date - offsets.DateOffset(months=12 * year)]
```

### 优先级 P3：数据库层面优化

1. 确保 `trans_report_flow` 表的 `out_req_no` 列有索引
2. 确保 `trans_account` 表的 `file_id`、`account_name + id_card_no + bank + account_no` 有联合索引
3. 适当增大连接池：`pool_size=20, max_overflow=20`

### 优先级 P3：View 层并行化

`V51001` 中 20+ 个视图处理器可以分组并行执行（注意共享数据的线程安全）：



```
from concurrent.futures import ThreadPoolExecutor  
  
with ThreadPoolExecutor(max_workers=4) as executor:  
    futures = []  
    for view in view_handle_list:  
        view.init(self.variables, self.user_name, self.id_card_no, self.origin_data, self.cached_data)  
        futures.append(executor.submit(view.process))  
    for f in futures:  
        f.result()
```

------

## 三、优化优先级总结

| 优先级 | 优化项                              | 预期效果              |
| :----- | :---------------------------------- | :-------------------- |
| P0     | 消除 `trans_report_flow` 重复查询   | 减少 4 次大表全量查询 |
| P0     | `SELECT *` 改为指定列               | 减少 30-50% 数据传输  |
| P1     | `apply(axis=1)` 改向量化            | 单步提速 10-100x      |
| P1     | `_in_out_order` 用 map 替代循环 loc | 单步提速 ~20x         |
| P2     | 缓存 `label_logic` 字典表           | 减少重复 DB 查询      |
| P2     | `_time_interval` 去掉 copy          | 减少内存分配          |
| P3     | 数据库加索引                        | 查询提速              |
| P3     | View 层并行化                       | 整体提速 2-4x         |