#  性能瓶颈分析报告2.0                                                                                                    

  通过分析整个代码库，我发现报告生成慢的核心原因集中在以下 5个方面：                                                    
  ---
  1. 重复数据库查询（最高优先级）

  单次报告生成会执行约 23次 SQL 查询，其中同一张表被反复查询：

  ┌───────────────────┬─────────────────────────────────────────────────────┬──────────┐
  │       表名        │                      查询次数                       │ 浪费程度 │
  ├───────────────────┼─────────────────────────────────────────────────────┼──────────┤
  │ trans_account     │ 8次以上                                             │ 极高     │
  ├───────────────────┼─────────────────────────────────────────────────────┼──────────┤
  │ trans_report_flow │ 2次（1次在TransFlow.init，1次在confidence_analyse） │ 高       │
  ├───────────────────┼─────────────────────────────────────────────────────┼──────────┤
  │ label_logic       │ 每次调用 get_trans_u_flow_portrait 都查             │ 中       │
  └───────────────────┴─────────────────────────────────────────────────────┴──────────┘

  关键问题：JsonUnionConfidenceAnalyse 在 TransFlow 已经加载了 trans_report_flow
  的情况下又重新查了一次（json_u_confidence_analyse.py:66-67）。

---
  2. df.apply(axis=1) 行级循环（高优先级）

  在大量流水数据下，apply(axis=1) 是 pandas 中最慢的操作之一，因为它退化为 Python 层的逐行循环。

  TransFlow.py:172-176 — 流水数据处理核心路径：
  df['unusual_trans_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0203' else None, axis=1)
  df['loan_type'] = df.apply(lambda x: x['label1'] if x['uni_type'] == '0202' else None, axis=1)
  df['is_sensitive'] = df.apply(...)
  这些可以用 np.where() 或布尔索引替换，速度提升 10-100倍。

  类似问题还出现在 json_u_normal_income_portrait.py:140-141,
  175-177、json_u_unusual_trans.py:58-61、json_u_loan_portrait.py:110-112 等文件中。

---
  3. 循环 + 全表扫描模式（高优先级）

  TransFlow.py:283-298 — _in_out_order 方法：
  for i in range(len(income_per_cnt_list)):
      df.loc[df['opponent_name'] == income_per_cnt_list[i], 'income_cnt_order'] = i + 1
  # ... 共8个循环，每个20次迭代 = 160次全表扫描
  修复方法：使用字典 map，O(1) 替代 O(n)：
  df['income_cnt_order'] = df['opponent_name'].map(
      dict(zip(income_per_cnt_list, range(1, len(income_per_cnt_list) + 1)))
  )

---
  4. DataFrame 深拷贝重复（中优先级）

  v51001.py 中 19个视图模块 几乎每个都调用 self.trans_u_flow_portrait.copy()，导致大数据集在内存中重复复制约 10次。

  涉及文件：json_u_normal_income_portrait.py:41、json_u_funds_summary_portrait.py:28、json_u_balance_trend_chart.py:16、
  json_u_industry_portrait.py:27 等。

---
  5. 顺序执行 + SQL 字符串拼接

  - 19个视图模块串行执行：每个模块都要几百毫秒到几秒，累加效应明显
  - SQL 字符串拼接（TransFlow.py:145）：使用 % 拼接 out_req_no 而非参数化查询，数据库无法复用执行计划

---
  优化建议优先级排序

  ┌────────┬───────────────────────────────────────────────────────────────┬────────────────────────┬────────┐
  │ 优先级 │                            优化项                             │        预估效果        │ 改动量 │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P0     │ 缓存 trans_account 查询结果，避免重复查库                     │ 减少 ~50% 数据库查询   │ 小     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P0     │ _in_out_order 用 dict.map 替换循环+loc                        │ 减少大量全表扫描       │ 小     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P0     │ apply(axis=1) 替换为 np.where 或向量化                        │ 提速 10-100x 关键路径  │ 中     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P1     │ 消除 JsonUnionConfidenceAnalyse 的冗余 trans_report_flow 查询 │ 减少一次大表全表扫描   │ 小     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P1     │ label_logic 查询结果缓存在 TransFlow 级别                     │ 减少1次查询/实体       │ 小     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P2     │ 视图模块中用 .copy() 改为按需选择列，避免全量复制             │ 减少内存占用           │ 中     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P2     │ SQL 参数化，用 %(out_req_no_list)s 替代 % 拼接                │ 使数据库能复用执行计划 │ 小     │
  ├────────┼───────────────────────────────────────────────────────────────┼────────────────────────┼────────┤
  │ P3     │ 将独立模块改为并发执行（concurrent.futures）                  │ 利用多核加速           │ 中     │
  └────────┴───────────────────────────────────────────────────────────────┴────────────────────────┴────────┘

  其中最核心的几个优化点（P0）改动量小、收益大，可以先从它们入手。需要我帮你实施其中的某些优化吗？