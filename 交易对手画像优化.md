总结
                                                                                                                          为什么数据一多就慢（根源分析）

  瓶颈: O(n²) 循环
  位置: get_amt_order() L139-146
  原因: 对每个交易对手名进行 df[df.name == x] 全表扫描 + drop() 复制, 交易对手上千时极慢
  复杂度: O(n²)
  ────────────────────────────────────────
  瓶颈: 重复类型转换
  位置: read_u_counterparty_pt() L23/29
  原因: pd.to_datetime() 对同一列执行了 2 次
  复杂度: O(2n)
  ────────────────────────────────────────
  瓶颈: 逐行 apply
  位置: read_u_counterparty_pt() L77
  原因: op_name_trans() 用 .apply() 逐行正则处理
  复杂度: O(n) Python 循环
  ────────────────────────────────────────
  瓶颈: join 错误
  位置: read_u_counterparty_pt() L31
  原因: ''.join(UNUSUAL_OPPO_NAME) 少写管道符 |，好在列表只有 1 个元素未触发 bug
  复杂度: -
  ────────────────────────────────────────
  瓶颈: 代码重复
  位置: in_out_detail() vs superposition_detail()
  原因: 2 个 75 行的方法 90% 逻辑相同
  复杂度: 维护成本

  已应用的 4 项优化

  1. O(n²) → O(n) 向量化（最大收益）
  - 改前：遍历每个交叉对手名 → 反复 df[df.name == x] 过滤 → 逐行 drop()
  - 改后：groupby().sum() 一次性算出所有对手的收支总额 → isin() 布尔索引一次过滤

  2. 缓存 pd.to_datetime
  - 将 pd.to_datetime() 结果存入变量复用，避免二次全表转换
  - 同时缓存 opponent_name.astype(str) 结果，供后续 .str.isnumeric() 和 .str.contains() 复用

  3. 向量化对手名处理
  - 中文字符提取：.apply(op_name_trans) → .str.replace(regex)，从 Python 循环变为 C 级向量化操作
  - HTML 实体解码：先 .str.contains() 快速检查是否存在 &#xxx;，仅对包含实体的行做 apply（罕见情况）

  4. 合并重复代码 + 优化明细循环
  - in_out_detail() / superposition_detail() → 合并为 _build_detail(df, is_superposition)
  - 按月明细查询：预构建 {name: group} dict 避免循环内重复 df[df.name == x] 过滤
  - 汇总行排名取数：df[df.col == val].iloc[0] → df.iloc[order]