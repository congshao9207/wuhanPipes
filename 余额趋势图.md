---                                                                                                                     性能瓶颈分析

  瓶颈 1（最严重）：嵌套循环中重复 DataFrame 过滤 — balance_trend_chart() L50-67

  for name in account_info['account_name'].unique():       # 外循环
      account_df = df.loc[df.account_id.isin(id_list_all)]  # 每次全表过滤
      for account_no in account_df['account_no'].unique():  # 内循环
          account_no_df = account_df.loc[df.account_no == x] # 再次过滤

  - 每层循环都做 df.loc[condition] 全表扫描，复杂度 O(账户数 × 账户数 × n)
  - account_info 已有账号与账户名的映射关系，但没利用起来，每次都得从 df 里重新过滤

  瓶颈 2（严重）：groupby 聚合函数内全表扫描 — get_account_balance_trend() func2 L90

  def func2(x):
      return x[x.index.isin(
          flow.loc[flow.usual_trans_type.str.contains('理财')].index.tolist()
      )].sum()

  - str.contains('理财') 扫描整个 DataFrame，结果对于每个 group（每一天）都相同
  - 但代码把它放在聚合函数里，导致这个 O(n) 操作被执行了 n_days × n_accounts 次
  - 假设 365 天 × 5 个账户 = 1825 次全表扫描，每次都是无意义的重复

  瓶颈 3：冗余 pd.to_datetime 转换 — get_u_flow_portrait_detail() L20/24

  同一列转换了 2 次。

  ---
  3 项优化总结

  优化项: 嵌套循环 → merge + groupby
  改前: 双重 Python for 循环 + 每次全表过滤
  改后: df.merge(account_info) 一次关联 + pandas groupby
  复杂度降低: O(k×m×n) → O(n)
  ────────────────────────────────────────
  优化项: 聚合内全表扫描 → 向量化分离
  改前: func2 在每个 group 内重复 str.contains('理财') 全表扫描
  改后: finance_mask 提前计算一次 + 向量化 groupby sum
  复杂度降低: O(g×n) → O(n)
  ────────────────────────────────────────
  优化项: 冗余日期转换
  改前: 2 次 pd.to_datetime
  改后: 缓存复用 1 次
  复杂度降低: O(2n) → O(n)